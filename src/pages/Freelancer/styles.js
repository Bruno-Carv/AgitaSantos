import Styled from 'styled-components/native';
import Contant from 'expo-constants';

export const View = Styled.View`
    padding-top: ${Contant.statusBarHeight + 20}px;
`;

export const Text = Styled.Text``;